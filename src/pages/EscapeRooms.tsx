import EscapeRoomsDescription from "@/features/escape-rooms/components/EscapeRoomsDescription";
import TicketTypes from "@/features/escape-rooms/components/TicketTypes";
import TicketsTypes from "@/features/home/components/TicketsTypes";

const EscapeRooms = () => {
  return (
    <>
      <EscapeRoomsDescription />
      <TicketsTypes />
      <TicketTypes />
    </>
  );
};

export default EscapeRooms;
